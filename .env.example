# Local Express server (npm run dev:server) uses SQLite in server/data/
# Vercel production: add Neon Postgres via Vercel → Storage → Connect Database.
# Vercel injects DATABASE_URL (or POSTGRES_URL) when linked.

DATABASE_URL=
POSTGRES_URL=
ADMIN_SECRET=
